// AUTO-GENERATED PLACEHOLDER.
// Run `pnpm db:types` (which calls `supabase gen types typescript --local`)
// after starting the local stack to overwrite this file with the real schema
// types. Kept as an explicit `any` alias so the project type-checks before the
// database is ever started.
export type Database = any;
