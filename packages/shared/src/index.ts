// ---------------------------------------------------------------------------
// @ebizz/shared — domain enums and DTO shapes shared by the API and the web app.
// Keep this framework-agnostic (no NestJS / React imports) so both sides can use it.
// ---------------------------------------------------------------------------

export * from "./enums";
export * from "./dtos";
